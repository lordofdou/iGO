var express = require('express');
var router = express.Router();


router.post('/', function(req, res, next) {
  // res.render('res');
  // res.send(req.body.admin + req.body.password);
  // res.send("sss");
  
});

module.exports = router;